# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
/build/
dist/
*.egg-info/
.eggs/

# Environment
.env
.envrc

# IDE
.idea/
.vscode/
*.swp
*.swo

# Notebooks
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Project-specific
results/
*.mp4
*.log
# Ignore generated GIFs in results/ but keep curated figures under docs/
results/*.gif

# DICOM raw data (kept outside git: large files)
data/raw/
*.dcm
*.IMA
data/*/
!data/README.md
!data/.gitkeep

# 3D Slicer scenes
*.mrml
*.mrb
*.nrrd
*.nii
*.nii.gz

# Note: docs/submissions/intermediate/build/*.pdf are tracked on purpose
# (the rendered PDFs are part of what the professor can browse on the repo).

# Local planning artifacts — not shared with the public repo
openspec/
# Local AI runtime state
.atl/

# AI segmentation: manual bbox config (example template stays tracked)
data/tumor_bbox.yaml
!data/tumor_bbox.yaml.example

# Vendored external AI dependency — cloned locally, not committed
external/MedSAM2/

# Transient NIfTI exports produced during AI segmentation pipeline
results/*.nii.gz

# Local agent guidance — not shared with the public repo
CLAUDE.md

# Testing infrastructure stays local (development-only, not part of submission)
tests/
pytest.ini

# Speaker scripts for the oral presentation — local only
notebooks/demo_script_*.md
